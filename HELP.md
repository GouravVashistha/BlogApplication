# 🔐 Steps to Implement JWT Authentication in Spring Boot

This project demonstrates how to implement **JWT (JSON Web Token) authentication** in a Spring Boot application.

It provides REST APIs for:

- User Registration
- User Login
- Accessing Protected Resources using JWT

---

## 📦 1. Add Dependencies

Add the following dependencies to your project:

- `io.jsonwebtoken:jjwt`
- `spring-boot-starter-security`
- `spring-boot-starter-web`
- `spring-boot-starter-data-jpa`

---

## ⚙️ 2. Create JWT AuthenticationEntryPoint

Create a class that implements:

```java
AuthenticationEntryPoint implments
AuthenticationEntryPoint
```

## ⚙ 3. Create JWTTokenHelper

## 📦4. 